// Package controlpb contains the generated protobuf types for the
// godotnet control plane (login, UDP handshake, ping/pong). Regenerate
// via `go generate ./...` from the repository root.
//
//go:generate protoc --go_out=. --go_opt=paths=source_relative control.proto
package controlpb
